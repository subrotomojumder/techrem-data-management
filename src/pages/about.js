import Private from '@/utils/Private';
import React from 'react';

const About = () => {
    return (
        <div>
            <h1 className='my-10 text-center text-4xl'>Private about page</h1>
        </div>
    );
};

export default Private(About);